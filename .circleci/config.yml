version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: echo "Terraform実行"
      - run:
        name: Terraform plan実行
        command: make terraform_plan
